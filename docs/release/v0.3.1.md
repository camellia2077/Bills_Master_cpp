## [v0.3.1] - 2026-02-28
### Summary
- 完成“统一模型优先 + 标准 JSON 并行输出”线性计划收口，执行状态推进至已完成（L8）。
- 默认导出流水线正式切换为 `model-first`，并保留 `legacy/json-first` 作为回退与对照窗口。
- 一致性门禁与性能门禁完成固化，`md/json/tex/typ` 范围的双通路对比通过。

### Refactor
- `ReportExportService` 收敛月报/年报重复的 pipeline + format 路由逻辑：
  - 抽取统一路由选择函数（标准渲染模式选择）。
  - 抽取统一渲染分派函数（markdown/latex/typst）。
  - 降低重复分支，保持行为不变。
- 并行测试运行目录命名缩短（`tools/build/build_then_cli_test.py`）：
  - 解决 Windows 路径过长导致的偶发目录创建失败。
  - 保留可追踪性（时间戳 + 缩短标签 + 指纹）。

### Pipeline & Gate
- 导出流水线状态：
  - 默认：`model-first`
  - 回退：`legacy`
  - 对照：`json-first`
- 门禁执行结果：
  - `python tools/verify/verify.py` 通过。
  - `python tools/verify/verify.py bills-parallel-smoke` 通过。
  - `python tools/verify/verify.py report-consistency-gate` 通过。
- 备注：过程中出现一次外部网络下载波动（FetchContent 下载 sqlite 中断），重试后通过，不属于业务逻辑回归。

### Docs
- 更新线性计划文档：`temp/统一生成标准化 JSON.md`
  - 记录 L6/L7/L8 完成情况。
  - 看板状态更新为“本计划已完成”。
- 更新 `libs/bills_core/README.md`：
  - 增补导出流水线状态与 legacy 下线前置条件。
- 更新 `docs/modules/bills_core/build_and_test.md`：
  - 增补一致性门禁命令与默认性能阈值说明。
  - 增补测试目录与输出目录 `logic/artifact` 分组说明。

### Test Layout
- 测试入口分层落地：
  - 逻辑层脚本：`tests/suites/logic/bills_core_abi/run_tests.py`
  - 产物层脚本：`tests/suites/artifact/bills_master/run_tests.py`
- 输出目录分层落地：
  - 产物输出迁移为：`tests/output/artifact/<project>/...`
  - 逻辑输出目录预留：`tests/output/logic/`
- 兼容入口保留：
  - `tools/verify/test_bills_core_abi.py` 保留为转发入口（内部转到 logic 分组脚本）。
  - `tests/suites/bills_master/run_tests.py` 保留为转发入口（内部转到 artifact 分组脚本）。

### Legacy 下线前置条件
- 连续通过一致性门禁（`report-consistency-gate`）且无数据差异。
- `model-first` 与 `json-first` 在 `md/json/tex/typ` 范围稳定一致。
- 回归窗口内无阻塞级问题依赖 `legacy` 回退。
- 已发布迁移说明并明确移除版本与回滚策略。

---

### 补充记录（2026-03-01）：Phase 3 清理与收口

- 完成 `bills_cli -> io/presentation` 分层重构收口（Phase 3）。
- 代码清理：
  - 移除多处无效依赖注入残留（`JsonConfigProvider`、`FileBillFileEnumerator`、`PathBuilder` 的未使用成员/构造参数）。
  - 删除 `products/bills_cli/src` 下迁移后空目录骨架（`adapters/serialization`、`db_insert/*`、`reports/core|monthly_report|yearly_report`）。
  - 移除内部过渡转发 API：删除 `AppController::handle_modification` 与 `WorkflowController::handle_modification`，`modify` 命令直接绑定 `handle_convert`。
- 文档收口：
  - 更新 `docs/modules/bills_core/architecture.md`、`module_map.md`、`build_and_test.md` 分层与门禁说明。
- 验证：
  - `python tools/verify/verify.py bills-build -- build_fast` 通过；
  - `python tools/verify/verify.py artifact-tests -- --scope basic` 通过；
  - `python tools/verify/verify.py all-tests` 通过。
