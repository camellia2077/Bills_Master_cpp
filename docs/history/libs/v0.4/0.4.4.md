## [v0.4.4] - 2026-04-01

### 新增功能 (Added)
* 新增对账单金额字段中加减法数学表达式（如 `3-4+7+10`）的解析支持。
* 实现基于 `Parent Category` 名称（如 `income`）与表达式前导符号的正负号自动判定逻辑。

### 技术改进/重构 (Changed/Refactor)
* 升级 `libs/core/src/ingest/transform/bills_parser.cpp` 中的正则表达式，以支持捕获数学表达式。
* 为 `libs/core` 解析引擎增加详细的英文注释，明确金额计算流程与符号判定意图。