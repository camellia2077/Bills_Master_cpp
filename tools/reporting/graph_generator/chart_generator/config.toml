# config.toml

[database]
# 相对路径基于当前 config.toml 所在目录解析
path = '../../../../tests/output/runtime/bills_tracer/workspace/bills.sqlite3'

[visualization]
# Matplotlib 使用的字体列表。程序会依次尝试，直到找到可用字体。
font_list = ["SimHei", "Microsoft YaHei", "PingFang SC", "WenQuanYi Micro Hei"]
