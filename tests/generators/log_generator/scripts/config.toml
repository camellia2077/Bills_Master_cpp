[dist]
# Optional: "Ninja" or "Unix Makefiles"
generator = "Ninja"

# Optional: "clang" (leave empty to use the repository default)
compiler = "clang"

# Path to the directory containing the top-level CMakeLists.txt
cmake_source_dir = "."
