[dist]
# Optional: "Ninja" or "Unix Makefiles"
generator = "Ninja"

# Optional: "clang" or "gcc" (leave empty to use CMake default)
compiler = "clang"

# Path to the directory containing the top-level CMakeLists.txt
cmake_source_dir = "."
